import React from "react";
// Styled components
import { StyledProfileSkills } from "@styles/components/profileSkills";

export const ProfileSkills: React.FC = ({ children }) => {
  return <StyledProfileSkills>{children}</StyledProfileSkills>;
};
